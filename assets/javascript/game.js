// when player clicks "click here" randomly picks a movie

// sets hangman

// sets new instructions

// sets answer field

// displays wrong guesses

const movies = ["predator","the terminator","conan the barbarian","true lies","commando","total recall","last action hero","the running man","twins","jingle all the way"];
let randMovie = movies[Math.floor(Math.random() * movies.length)];
